import CameraBaseState from './CameraBaseState';

export default class CameraStaticState extends CameraBaseState {
    enterState(currCamera) {
        super.enterState(currCamera);
    }

    exitState(currCamera) {
        super.exitState(currCamera);
    }

    renderCamera(currCamera) {
        super.renderCamera(currCamera);
    }

    updateState(currCamera) {
        super.updateState(currCamera);
    }
}
